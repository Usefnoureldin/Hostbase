---
const plans = [
  {
    name: 'Individual Growth',
    price: 99,
    unit: '/ month',
    description: 'Built for independent operators. Go from zero to live on Booking.com in minutes.',
    features: [
      'Unlimited listings',
      'Supports Booking.com only',
      'Intelligent room auto-shuffle system',
      'Smart calendar & reservations',
      'Guest messaging inbox',
      'Revenue analytics dashboard',
      'Email notifications',
    ],
    cta: 'Try for Free',
    highlighted: false,
  },
  {
    name: 'Pro',
    price: 399,
    unit: '/ month',
    slider: {
      min: 1,
      max: 5,
      default: 1,
      pricePerProperty: { 1: 399, 2: 349, 3: 329, 4: 299, 5: 249 },
    },
    description: 'For operators with up to 5 properties. Manage and grow your portfolio across every channel. Rates sync automatically, AI optimizes the rest. You handle the guests.',
    introLine: 'Everything in Individual Growth, plus...',
    features: [
      'Channel manager (20+ OTAs)',
      'White-label subdomain',
      'Weekday/weekend/off-peak rates',
      'Derived rate plans with markup',
      'Priority support',
    ],
    cta: 'Get Now',
    highlighted: true,
  },
  {
    name: 'Enterprise',
    price: null,
    unit: '',
    description: 'For operators managing 5+ properties. Dynamic pricing, AI-powered operations, landlord reporting, and full white-label branding on your own domain. Everything tailored to your portfolio.',
    introLine: 'Everything in Pro, plus...',
    features: [
      'Dynamic pricing algorithm',
      'Claude + ChatGPT MCP',
      'Custom Landlord View',
      'Full white-label branding',
      'Dedicated onboarding',
      'Custom domain',
    ],
    cta: 'Request a Demo',
    highlighted: false,
  },
];
---

<section id="pricing" class="py-20 md:py-28 bg-surface-alt">
  <div class="max-w-6xl mx-auto px-6">
    <!-- Section header -->
    <div class="text-center max-w-2xl mx-auto mb-16">
      <p class="text-sm font-semibold text-primary tracking-wide uppercase mb-3">Pricing</p>
      <h2 class="text-3xl md:text-4xl font-bold tracking-tight text-text font-display">
        Pricing that rewards scale.
      </h2>
      <p class="mt-4 text-text-muted text-lg">
        One flat rate per property. Unlimited listings, unlimited rooms. No hidden fees. Cancel anytime.
      </p>
    </div>

    <!-- Pricing cards -->
    <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
      {plans.map((plan) => (
        <div class={`relative rounded-2xl p-8 flex flex-col transition-all duration-300 ${
          plan.highlighted
            ? 'bg-white border-2 border-primary shadow-xl shadow-primary/10 scale-[1.02]'
            : 'bg-white border border-border hover:border-primary/30 hover:shadow-lg'
        }`}>
          {plan.highlighted && (
            <div class="absolute -top-3.5 left-1/2 -translate-x-1/2">
              <span class="px-4 py-1 bg-primary text-white text-xs font-semibold rounded-full">Most Popular</span>
            </div>
          )}

          <div class="mb-6">
            <h3 class="text-2xl md:text-3xl font-bold tracking-tight text-text">{plan.name}</h3>
          </div>

          <div class="mb-6">
            <div class="flex items-baseline gap-1">
              {plan.price !== null ? (
                <>
                  <span
                    class="text-4xl font-bold text-text"
                    data-price-display={plan.slider ? 'pro' : undefined}
                    data-price-tiers={plan.slider ? JSON.stringify(plan.slider.pricePerProperty) : undefined}
                  >${plan.slider ? (plan.slider.default * plan.slider.pricePerProperty[plan.slider.default]).toLocaleString() : plan.price}</span>
                  <span class="text-text-muted text-sm">{plan.unit}</span>
                </>
              ) : (
                <span class="text-4xl font-bold text-text">Custom</span>
              )}
            </div>
            {plan.slider && (
              <>
                <p class="text-sm text-text-muted mt-1">
                  <span data-per-property>${plan.slider.pricePerProperty[plan.slider.default]}</span> / property
                </p>
                <div class="mt-4">
                  <div class="flex items-center justify-between mb-2">
                    <label for="pro-properties" class="text-sm text-text-muted">Properties</label>
                    <span class="text-sm font-semibold text-text">
                      <span data-properties-count>{plan.slider.default}</span>
                      <span data-properties-label>{plan.slider.default === 1 ? 'property' : 'properties'}</span>
                    </span>
                  </div>
                  <input
                    id="pro-properties"
                    type="range"
                    min={plan.slider.min}
                    max={plan.slider.max}
                    value={plan.slider.default}
                    class="w-full accent-primary cursor-pointer"
                    aria-label="Number of properties"
                  />
                  <div class="flex justify-between mt-1 text-xs text-text-muted">
                    <span>{plan.slider.min}</span>
                    <span>{plan.slider.max}</span>
                  </div>
                </div>
              </>
            )}
            <p class="text-sm text-text-muted mt-3">{plan.description}</p>
          </div>

          <div class="flex-1 mb-8">
            {plan.introLine && (
              <p class="text-base font-bold italic text-text-muted mb-4">{plan.introLine}</p>
            )}
            <ul class="space-y-3">
            {plan.features.map((feature) => (
              <li class="flex items-start gap-3 text-sm text-text">
                <svg class="w-5 h-5 text-primary shrink-0 mt-0.5" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
                {feature}
              </li>
            ))}
            </ul>
          </div>

          <a
            href="mailto:founders@hostbase.ai?subject=Hostbase Demo Request"
            class={`inline-flex items-center justify-center py-3 px-6 rounded-xl font-medium text-sm transition-all ${
              plan.highlighted
                ? 'bg-primary text-white hover:bg-primary-dark shadow-lg shadow-primary/25'
                : 'bg-surface-alt text-text hover:bg-primary/10 hover:text-primary border border-border'
            }`}
          >
            {plan.cta}
          </a>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  const slider = document.getElementById('pro-properties') as HTMLInputElement | null;
  const priceEl = document.querySelector<HTMLElement>('[data-price-display="pro"]');
  const perPropEl = document.querySelector<HTMLElement>('[data-per-property]');
  const countEl = document.querySelector<HTMLElement>('[data-properties-count]');
  const labelEl = document.querySelector<HTMLElement>('[data-properties-label]');

  if (slider && priceEl && perPropEl && countEl && labelEl) {
    const tiers: Record<string, number> = JSON.parse(priceEl.dataset.priceTiers || '{}');
    const update = () => {
      const n = Number(slider.value);
      const perProp = tiers[String(n)] ?? 0;
      priceEl.textContent = '$' + (n * perProp).toLocaleString();
      perPropEl.textContent = '$' + perProp;
      countEl.textContent = String(n);
      labelEl.textContent = n === 1 ? 'property' : 'properties';
    };
    slider.addEventListener('input', update);
    update();
  }
</script>
